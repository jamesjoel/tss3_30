import { TotalSalaryPipe } from './total-salary.pipe';

describe('TotalSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
